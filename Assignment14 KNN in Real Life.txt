from math import sqrt

# Example users
A = [5, 3, 2]
B = [4, 3, 1]
C = [1, 5, 4]

# Euclidean distance
def distance(x, y):
    return sqrt(sum((a-b)**2 for a, b in zip(x, y)))

print("Distance A-B:", distance(A, B))
print("Distance A-C:", distance(A, C))